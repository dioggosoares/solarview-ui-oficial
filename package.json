{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "docs": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@primittive/eslint-config": "^1.0.5",
    "eslint": "^8.34.0",
    "turbo": "^1.8.1"
  }
}
